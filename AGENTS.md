# 策略迭代约定

本项目唯一策略迭代标准文件是 `CURRENT_STRATEGY.md`。开始策略相关工作前先阅读它。

每轮修改必须：

1. 复盘上一轮预期与实际证据。区分用户反馈、代码检查、离线回归和实战结果；没有实战证据时写“待验证”，不能把测试通过写成实战有效。
2. 更新当前规则，并在该文件的迭代记录末尾追加新条目，用 `---` 分隔；保留历史条目。
3. 每条记录写清：为什么变更、具体修改、预期效果、验证结果、已知局限、下一轮需要观察什么及未达预期时如何改进。
4. 行为改动补充对应回归测试，并记录实际运行结果。后续发现预期未达成时，记录原因和新改进，持续循环。

基础约束：默认三面墙、后侧留通道；开局三座火箭塔共用一个空置且可达的控制位；工人经济目标跨回合保持稳定；夜间允许安全采矿，所有移动路径及降级出口遵守避险要求。
