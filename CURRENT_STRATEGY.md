# 当前策略与迭代记录

更新日期：2026-09-21，迭代 002。本文件是项目唯一策略迭代标准文件；历史版本文档与此冲突时，以本文件和对应回归测试为准。

## 每轮迭代要求

先复盘上一轮预期与实际证据，再修改当前规则；每轮在文末追加记录，以 `---` 分隔，不覆盖历史。每条必须包含：**为什么变更、做了哪些修改、预期效果、实际验证、局限、下轮观察与改进方向**。用户反馈、代码推断、离线测试、实战结果必须区分，没有实战证据就标记“待验证”。`AGENTS.md` 提醒后续维护者遵循本流程。

## 术语与目标

火箭塔对应现有协议的 `rocket`。协议还支持 `gatling`、`railgun`，没有独立 `arrow` 类型。

默认前期使用 3 座一级火箭塔过渡。若接管的局面已有加特林或轨道炮，它们也计入 3 座武器目标，只在空位补火箭塔，不拆掉或覆盖已有武器。当前没有自动改建为混合武器的后期规则。

三座塔必须邻接同一个公共控制格，控制格不建塔、不建墙，固定障碍物占位时重新选择布局。规划纳入已有塔的位置，不能选一个已被塔占据的控制格。一个 Pioneer 留在公共格，按服务器返回的冷却状态每回合操作一座就绪火箭塔，利用三回合 CD 轮换三塔；不是同回合操作三座。正常新建布局的控制格随三塔逐步建成保持稳定。若遗留塔位无法组成合法公共控制布局，不盲目覆盖旧塔或捏造控制位。

## 开局与金币使用

- 默认建筑目录下，首个建设目标必须是火箭塔；武器不足 3 座时不生成建墙候选，也不购买、使用任何建筑升级券。
- 活着且 ID 按字符串排序最小的 Worker 为主建设者。白天金币至少 25、存在合法空位时，优先建火箭塔；尚未到位时优先前往武器施工位。建造动作优先于施工移动，两者均优先于普通采矿、卖矿、购物等动作，不受学习评分高低改变。
- 团队金币冲突时先支付建塔费用；主建设者正在前往武器施工位时，保留一座塔的 25 金币。其他角色只能花剩余金币，同回合卖矿收入不计入可花预算。
- 不足 25 金币时继续经济循环；夜间、没有合法空位或没有可达施工路径时，不能强行发出 build。此时采用其他合法候选。建造仍受建筑数量、占位、距离、资源和最终协议校验约束。
- 第二 Worker 开局优先采集 stone，为三塔后的三面墙建设备料。显式仅配置墙体配方的特殊地图保留独立建墙能力，不套用默认武器开局门槛。

## 武器与墙体升级

只有己方武器总数达到 3 座才开启升级计划。每回合根据真实建筑等级重新选择唯一目标；武器损失后先补数量，再恢复升级。

| 顺序 | 升级目标 | 使用道具 |
| --- | --- | --- |
| 1 | 所有武器升二级 | `WeaponUpgradeVoucher1` |
| 2 | 已有迎敌侧墙体升二级 | `WallUpgradeVoucher1` |
| 3 | 所有武器升三级 | `WeaponUpgradeVoucher2` |
| 4 | 已有迎敌侧墙体升三级 | `WallUpgradeVoucher2` |
| 5 | 基地升二级、再三级 | `StationUpgradeVoucher1/2` |
| 6 | 上下墙体升二级、再三级 | `WallUpgradeVoucher1/2` |
| 7 | 若遗留局面已有后侧／规划外墙，最后升级 | `WallUpgradeVoucher1/2` |

迎敌侧识别：左上／左半图基地优先右侧墙，右下／右半图基地优先左侧墙。代码以基地 footprint 中心相对地图中心的横坐标判断，沿用游戏坐标体系，不依赖屏幕纵轴方向。迎敌侧包含该侧两个角点，其余顶部、底部墙优先级较低。

不再等待三面墙全部建完才升级已有迎敌侧墙。某阶段没有对应墙体时直接跳过；后来补建一级迎敌墙会重新进入优先升级队列。同一阶段按建筑 ID 字符串排序，避免目标漂移。

买券、持券接近目标、使用券共同读取 `next_upgrade_target()`。买不起当前目标的券时继续赚钱，不生成无意义的购券行程，也不改买低优先级墙体升级券。建筑升级顺序是确定规则；升级事务与采矿、墙体施工、维修等动作之间仍由现有收益评分选择。

## 围墙施工

三座武器后按三面墙 blueprint 施工：左侧基地建设上、下、右侧；右侧基地建设上、下、左侧，后侧保留出入口。标准 2×2 基地对应 16 格墙位。

**不要求四面墙。** 左上基地的左侧、右下基地的右侧属于低优先级后侧。后侧上下端点属于上下墙，后侧中间四格留空；不以协议的 20 面墙上限作为施工目标。

施工顺序仍为迎敌侧中间四格→迎敌侧角点→上下交替延伸。施工导航与实际落墙共享有序目标。缺墙期间保留所需 stone；保留既有危险墙重建、维修及夜间安全导航机制。

## 工人经济行程

采矿与售卖使用统一的转换条件：达到非石材批量（默认 4）、背包高水位、夜前矿物返场阈值或已无可采资源时，才开启有货可卖的售卖行程。不得仅因背包有 1 块矿就从采矿途中掉头找商人。

售卖行程开始后记住商人坐标，优先完成导航和卖矿；卖掉一种矿后，剩下的可售矿石即使不足一批也继续卖完。只有观察到可售库存清空、商人消失或角色死亡，才释放售卖承诺。夜间暂停卖矿行程，允许安全采矿；白天再恢复。选定商人不可达时尝试其他商人。

采矿目标不再在 4 回合后自动失效：只要仍可达就持续前往，不因途经更值钱／更近的矿而改道；目标枯竭、不可达或变危险时允许改选。旧 `resource_commitment_min_rounds` 配置保留兼容，但不再用于自动释放有效矿点。卖矿、开局建塔和夜前施工等明确阶段转换仍可中断采矿。每回合重新计算路径，承诺的是目标而非固定路线。售卖暂时无路可走时不靠随机移动或重新采矿打断行程。

## 工人夜间采矿与安全寻路

夜间开放采矿，边缘和内部矿都按相同安全标准检查。工人不因进入夜晚就被拉回塔位，Pioneer 负责公共控制位。

1. 去程使用八方向、带 ETA 的 A*，遵守建筑、矿物、任务点、角色占位和已学会的障碍规则；每步同时检查回合切换前后的机器人威胁。
2. 威胁取机器人当前所在区域、向基地预测路线、最近真实行进方向预测的并集。机器人可能停下来攻击，不能仅凭预测“它会走开”就把其当前攻击区当安全格。预测超过预生成长度后继续推演，不冻结在第 12 回合。
3. A* 的终点条件同时包括到达时的风险、连续安全采集窗口、采集后能否撤到安全区。最近交互格不合格时继续搜索其他交互格，不能因此直接放弃整座矿。
4. 撤离终点按实际到达时间及后续驻留窗口验算；搜索两侧边缘全部合法安全区，不按坐标截取前 256 格。已经站在安全边缘时可以原地停留，无须为了“撤离”反复走动。
5. 采集每回合重新验证血量、停留窗口和撤离可行性。逼近威胁触发明确的撤离目标，优先于采矿；未受威胁且可安全采集时继续采集。
6. 普通探索不在夜间生成；Legal 检查和最终协议校验拦截进入机器人攻击区的工人移动。紧急降级同样只使用已验证的撤离／采集；没有安全动作时返回空角色指令并记录 `safety_hold`。

A* 计算的是完整多步路线，但受回合预算、最大步数和展开数约束；预算耗尽或找不到安全路线时拒绝出发。机器人预测仍是保守估计，不是服务器内部寻路器，也未精确模拟破墙时间；不能承诺工人在任意围堵状态下都能脱险。

## 迭代 001 检查发现与修复

| 原实现 | 迭代 001 修复 |
| --- | --- |
| Rocket→Gatling→Railgun 各一座 | 空位连续补 Rocket，达到 3 座才升级 |
| 近墙且携带 stone 时可能在三塔之前建墙 | 默认建筑目录下先完成三塔 |
| 建塔靠收益加分，其他动作或角色消费可能抢占 | 排序优先建造／施工导航，并保护建塔金币 |
| 基地升级插在武器升级之间，迎敌墙排在核心全满之后 | 武器与迎敌侧墙交替升二级、三级，然后基地与其他墙 |
| 墙体升级必须等待 blueprint 完整 | 已有迎敌墙即可升级 |
| 其他墙二级先于迎敌墙三级 | 迎敌墙三级先于其他墙二级 |

## 代码与验证入口

| 关注点 | 文件／函数 |
| --- | --- |
| 开局武器类型和数量 | `src/fortress_agent/game_rules/economy.py::next_weapon_build_type` |
| 默认建造配方、成本 | `src/fortress_agent/policy/build_catalog.py` |
| 建塔与墙体候选 | `src/fortress_agent/candidates/business.py::BuildCandidateGenerator` |
| 主建设者返场 | `src/fortress_agent/candidates/navigation.py::WeaponBuildApproachCandidateGenerator` |
| 开局动作排序 | `src/fortress_agent/policy/construction_priority.py`、`ranker.py` |
| 金币保护 | `src/fortress_agent/policy/team_constraints.py::GoldBudgetConstraint` |
| 基地方向、墙位 | `src/fortress_agent/game_rules/build_area.py` |
| 升级顺序 | `src/fortress_agent/game_rules/upgrades.py::next_upgrade_target` |
| 顺序、数量、方向、金币回归 | `tests/unit/test_v059_strategy_repairs.py` |
| 最终 build/use 指令回归 | `tests/integration/test_strategy_economy_prepare_defense.py` |
| 矿点／商人跨回合承诺 | `src/fortress_agent/memory/economy.py`、`policy/worker_priority.py` |
| 完整夜间路线及终点验算 | `src/fortress_agent/world/safe_pathfinding.py`、`game_rules/night_safety.py` |
| 夜间降级与最终防线 | `src/fortress_agent/safety/emergency.py`、`protocol/final_validator.py` |
| 本轮布局、售卖、避险回归 | `tests/unit/test_strategy_iteration.py` |

快速检查命令（Python 3.11，安装开发测试依赖后）：

```powershell
python -m pytest tests/unit/test_strategy_iteration.py tests/unit/test_v059_strategy_repairs.py tests/integration/test_strategy_economy_prepare_defense.py tests/integration/test_configured_build_recipe.py
```

迭代 001 验证：Python 3.11.14 下全量测试 **271 passed**，相关 Ruff 和 `git diff --check` 通过。本机原 `.venv` 的解释器路径失效，使用工作区 `tmp/` 下的隔离 Python 环境运行测试。最新验证见文末迭代记录。

实战查看日志时，核对主建设者的 `build rocket`、`weapon_build`、升级券 `buy/use`，以及每次 `lastCmdResult`。候选或发出指令只表示计划／尝试，建造与升级成功必须以下一回合服务器状态和反馈为准。

---

## 迭代 001｜2026-09-21｜建塔与迎敌墙升级（提交 a43a8b9）

**为什么变更：** 用户要求先建火箭塔、武器先满数量再升级、优先迎敌侧墙；原实现采用混合三塔且迎敌墙升级过晚。

**修改：** 连续补 Rocket 至 3 座；建造／返场优先排序和金币保护；武器与迎敌墙交替升级；移除墙体升级必须等三面墙完整的门槛。

**预期效果：** 有钱时先形成三塔防御，再强化武器和正面，减少预算被其他消费抢占。

**实际验证：** 271 项离线测试通过；已推送。无在线比赛验证。

**下一轮反馈与复盘：** 用户仍反馈采矿／售卖折返、夜间工人冒险；本轮未修改经济会话和紧急降级出口，不能指望建塔评分修复这些问题。旧测试只验证静态状态，缺少跨回合售卖和动态危险场景。迭代 002 对这些原因补充修复与测试，而不把未提供的实战日志当成已验证因果。

---

## 迭代 002｜2026-09-21｜三塔控制位、稳定经济行程与夜间安全采矿

**为什么变更：** 用户重申三面墙和单角色三塔轮换，并反馈工人在采矿／售卖间折返、夜间接近机器人送死。代码检查发现：售卖导航过早出现、矿点承诺过期、商人导航的真实终点与记录目标可能不同、旧控制格可能被塔占据、夜间紧急降级随机移动、路径终点缺乏到达时停留／撤离校验。

**为了这个目的做了哪些修改：**

- 保持 16 格三面墙蓝图与后侧中间通道；遗留后侧墙升级放到上下墙之后。公共控制位避开固定障碍及已有塔，三座塔保持共同邻接并按真实 CD 选择就绪塔。
- 引入商人坐标会话及统一出售条件；库存未卖完时保持售卖目标；矿点承诺维持到目标失效／不可达／安全条件变化，日间不可达时尝试下一候选。
- 安全 A* 增加动态终点谓词、回合边界风险、全边缘撤离目标、零步终点检查；采矿终点必须通过停留及撤离验证。保留机器人当前位置威胁，修正预测截断与软风险半径计算。
- 工人撤离作为明确目标参与排序，不再被低移动评分过滤；关闭夜间随机探索／降级移动，增加 Legal 与输出前的夜间防线。没有安全动作时正常返回 `safety_hold`，避免异常导致乱走。
- 增加 `worker_economy_sessions` 结构化事件，记录矿点与商人坐标，供下一轮对照角色轨迹和服务器反馈；增加仓库 `AGENTS.md` 固定迭代流程。

**预期效果：** 一次采矿／售卖行程不因中途评分变化往返；部分卖矿后卖完再离开；安全夜矿持续产出，敌人逼近时撤离；三塔由一个固定角色轮换，工人继续经济活动。

**实际验证：** Python 3.11.14 全量测试 **292 passed**。新增多回合生产 Runtime 回放已验证“去商人→卖第一类矿→继续卖剩余矿”和“夜间 collect→敌人逼近→安全撤离”；两侧基地三面墙、三塔逐座建设后控制格稳定、CD 候选、不可达矿改选、售卖堵路不折返、左右边缘及内部安全矿、危险降级拦截等测试通过。新增模块、夜间寻路和会话记忆的定向 Ruff 检查、`git diff --check` 通过。没有在线比赛结果，实战效果待验证。

**已知局限：** 无法确认真实机器人未来路线，静止威胁并集可能保守地放弃部分可采矿；预算不足时宁可安全等待。已有塔位置完全无法共用控制格时不会自动拆塔。经济会话只保存在当前进程内，重启后重新判断；被动态单位临时堵住的路径仍可能暂停。

**下一轮复盘指标与未达预期时的改进方向：**

| 观察证据 | 本轮预期 | 若仍未达到，下一步检查／改进 |
| --- | --- | --- |
| `worker_economy_sessions`、角色坐标、实际背包变化、move/sell 反馈 | 一次商人会话内目标稳定，卖完才恢复采矿 | 检查失败反馈、临时堵路与商人可达性；必要时加入无进展计数和重新选路，而不是直接加售卖收益 |
| 矿点承诺、每回合选中的矿、采集成功数 | 可达且安全的矿点不因经过更近矿而变化 | 检查枯竭识别、同伴占位与资源记忆；补充相应跨回合回放 |
| 夜间 HP、机器人轨迹、collect/move、安全等待次数 | 安全矿持续产出，逼近时不再采集或向攻击区移动 | 若受击，回放动作执行顺序与预测误差；若过度等待，检查预算耗尽、风险余量和撤离终点，不直接放开安全门槛 |
| 三塔实际位置、Pioneer 位置、服务器 CD、attack 成功反馈 | 共用控制位不移动，每回合至多操作一座就绪塔 | 检查服务器 CD 语义、控制位通路和目标射程；用连续多夜回放调整 |
| 墙位与升级券 use 反馈 | 默认无后侧中间墙，迎敌侧优先，上下其次 | 检查特殊地图覆盖配置和已有遗留墙，而非把 20 墙上限误作目标 |

下一次迭代先填写上述结果，再追加新的分隔记录；不得用本轮测试通过代替实战复盘。
